/* exported getFirstChar */
/*
- need to get first character so = index[0]
-if we have a string =''; need to grab the string[0], assign an empty string
*/

function getFirstChar(string) {
  string = string[0];
  return string;
}
