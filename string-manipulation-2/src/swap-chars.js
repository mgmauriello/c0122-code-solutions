/* exported swapChars
-swap the char at first index and second index in the string
-relace char at firstIndex with secondIndex

*/
function swapChars(firstIndex, secondIndex, string) {
  var newStr = string[firstIndex];
  return newStr;
}
