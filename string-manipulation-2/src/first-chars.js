/* exported firstChars
- find the first characters in the string
-count needs to start at 0 and then followed by the length of the string being returned

*/
function firstChars(length, string) {
  return string.substring(0, length);
}
