/* exported includes
-need to see if value is located in array
-with indexOf returns -1 if false
-get the value of the array and have it search array via indexOf

*/
function includes(array, value) {
  return array.indexOf(value) > -1;
}
