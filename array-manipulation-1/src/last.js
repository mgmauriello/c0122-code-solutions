/* exported last */
/*
-get the last string in the array
-find last by array.length - 1 ;
-ensure to have array find the new arr at last index
-then need to return the last index of array
*/
function last(array) {
  var arr = array.length - 1;
  var lastArr = array[arr];
  return lastArr;
}
