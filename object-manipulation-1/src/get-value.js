/* exported getValue */
/*
-need to get value of property of an obj into own array
-how to get the value of the property returned?
-grab object at the key and return
*/
function getValue(object, key) {
  return object[key];
}
